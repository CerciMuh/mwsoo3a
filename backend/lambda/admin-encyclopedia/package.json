{
  "name": "admin-encyclopedia-lambda",
  "version": "1.0.0",
  "description": "Lambda function for admin encyclopedia management (degrees and courses)",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && npm run zip",
    "zip": "powershell -Command \"if (Test-Path lambda.zip) { Remove-Item lambda.zip }; Compress-Archive -Path dist/*,node_modules -DestinationPath lambda.zip\"",
    "deploy": "aws lambda update-function-code --function-name AdminEncyclopedia --zip-file fileb://lambda.zip --region eu-central-1 --profile mwsoo3a"
  },
  "keywords": [
    "lambda",
    "dynamodb",
    "admin"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.645.0",
    "@aws-sdk/lib-dynamodb": "^3.645.0",
    "aws-jwt-verify": "^4.0.1",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^20.14.9",
    "@types/uuid": "^10.0.0",
    "typescript": "^5.5.3"
  }
}
